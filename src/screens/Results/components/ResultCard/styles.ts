import styled from 'styled-components/native'

export const Container = styled.View`
  flex-direction: row;
  background-color: rgba(41, 45, 92, 0.1);
  padding: 1px;
  gap: 1px;
  border-radius: 25px;
  overflow: hidden;
  box-sizing: border-box;
`

export const Wrapper = styled.View`
  gap: 15px;
`
